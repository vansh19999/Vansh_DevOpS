apiVersion: v1
kind: Service
metadata:
  name: web-stable-svc
  namespace: app-demo
  labels: { app: web, track: stable }
spec:
  selector: { app: web, track: stable }
  ports:
    - name: http
      port: 80
      targetPort: 8080
